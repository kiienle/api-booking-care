import db from "../models/index";

let getTopDoctorHome = () => {
    return new Promise(async (resolve, reject) => {
        try {
        } catch (e) {
            reject(e);
        }
    });
};

module.exports = {
    getTopDoctorHome,
};
