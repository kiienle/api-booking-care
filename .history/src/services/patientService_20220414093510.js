import db from "../models/index";
import _ from "lodash";

let postBookingAppointment = () => {
    return new Promise(async (resolve, reject) => {
        try {
        } catch (e) {
            console.log(e);
            reject(e);
        }
    });
};

module.exports = {
    postBookingAppointment,
};
